HEC-RAS project placeholder generated from test3.json
Model settings to apply in HEC-RAS:
- Steady flow: 800.0 cfs
- Boundary condition: normal_depth (value: 0.001)
- Suggested reach lengths taken from section metadata (L_* fields)

Files:
  XS_0.csv: station (offset), elevation pairs
  XS_212.csv: station (offset), elevation pairs
  XS_558.csv: station (offset), elevation pairs
  XS_740.csv: station (offset), elevation pairs
  XS_920.csv: station (offset), elevation pairs

Import notes:
1) In HEC-RAS Geometry -> Cross Sections -> Import GIS/CSV, import each XS_*.csv as a cross section. Use Offset as station (left bank = 0).
2) For each cross section, set left and right bank stations using left_bank_station/right_bank_station from sections_metadata.csv.
3) Set Manning n for left overbank, channel, and right overbank as listed in metadata.
4) Create a steady flow plan with flow = flow_cfs and apply normal depth boundary using slope (boundary_value) or specify normal depth if known.
