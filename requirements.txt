# HYDRA2DGPU Python Dependencies
# Core (required for all functionality)
numpy>=1.24

# Mesh generation (required for unstructured mesh workflow)
gmsh>=4.12

# Optional: result export formats
h5py>=3.8          # HEC-RAS HDF5 export
netCDF4>=1.6       # UGRID NetCDF export

# Optional: visualization
matplotlib>=3.7    # In-plugin plotting

# Documentation
pdoc>=14.0         # Auto-generated API reference docs

# Note: QGIS, PyQt5, and osgeo (GDAL) are provided by the QGIS installation
# and should NOT be installed via pip.
