"""QGIS attribute form init script for structures layer.
Kept as a no-op so setInitCodePath / setInitFunction don't error.
All widget configs are handled in topology_tab_view.py (workbench path)
or the QML file (standalone path).
"""


def form_open(dialog, layer, feature):
    pass
